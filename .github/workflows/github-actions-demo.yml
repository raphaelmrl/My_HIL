name: Build Application
on: 
  workflow_dispatch : {}


jobs:
  build:
    name: Build App
    runs-on : ubuntu-22.04
    steps : 
      - run: echo "tets"$
      
      - name: Checkout repository
        uses: actions/checkout@v4

      - run: ls

